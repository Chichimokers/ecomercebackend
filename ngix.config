  GNU nano 6.2                                                                   miapp.conf                                                                             server {
    listen 80;
    server_name esaki-jrr.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name esaki-jrr.com;

    # SSL Configuration (existente)
    ssl_certificate /etc/letsencrypt/live/esaki-jrr.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/esaki-jrr.com/privkey.pem;

    # Configuración de rutas
    location / {
        # Proxy al frontend (Next.js SSR)
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/v1/ {
        # Proxy al backend (NestJS)
        proxy_pass http://127.0.0.1:3100/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
